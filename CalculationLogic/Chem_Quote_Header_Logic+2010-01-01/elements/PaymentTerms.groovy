/**
 * User Story:
 * As a Sales Rep, I need to be able to select the PaymentTerms (from a dropdown) negotiated with Customer,
 * because it will be used to calculate Cash Discount provided to the customer in case they meet the PaymentTerms conditions.
 *
 * Acceptance Criteria / Implementation Details:
 * - the list of available Payment Terms options is in the Price Parameter table "CashDiscount"
 * - the input parameter is named "PaymentTerms"
 * - the PaymentTerms dropdown is displayed as input of the quote header (root folder)
 * - the user selects only one option (or none)
 */

// FIXME START

// FIXME END
