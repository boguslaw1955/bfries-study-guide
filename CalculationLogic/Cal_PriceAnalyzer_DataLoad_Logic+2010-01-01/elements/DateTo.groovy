/**
 * User Story:
 * As a Pricing Analyst, I need to be able to setup the final date, for which I require the conversion table (DS "cal") to be generated.
 *
 * Acceptance Criteria / Implementation Details
 * - this element creates a date input field "DateTo" to allow user to enter the final date for generated rows
 * - the label of the input field is "Date to".
 * - this elements returns the value entered by the user into input field "DateTo"
 */

// FIXME START

// FIXME END
