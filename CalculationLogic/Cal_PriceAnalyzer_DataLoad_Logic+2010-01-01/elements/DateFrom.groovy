/**
 * User Story:
 * As a Pricing Analyst, I need to be able to setup the first date, for which I require the conversion table (DS "cal") to be generated.
 *
 * Acceptance Criteria / Implementation Details
 * - this element creates a date input field "DateFrom" to allow user to enter the initial date for generated rows
 * - the label of the input field is "Date from".
 * - this elements returns the value entered by the user into the input field "DateFrom"
 */

// FIXME START

// FIXME END
