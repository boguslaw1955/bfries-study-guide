/**
 * User Story:
 * As a Pricing Manager, I need to be able to specify the Pricing Strategy in the system, so that the products prices
 * can be calculated according to different strategies.
 *
 * Acceptance Criteria / Implementation Details:
 * - This element returns the whole strategy data row from the Price Parameter table "PriceStrategy"
 * - the strategy depends on the product attribute "ProductGroup
 * - the Map returned from this element will be used in subsequent elements
 */

// FIXME START

// FIXME END
