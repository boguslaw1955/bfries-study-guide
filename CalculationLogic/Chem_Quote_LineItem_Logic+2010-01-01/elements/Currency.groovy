/**
 * User Story:
 * As a Sales Rep, I need to see the calculated prices in the currency used for this customer, so that I do not have
 * to recalculate it for the customer manually.
 *
 * Acceptance Criteria / Implementation Details:
 * - this element returns the currency symbol used for the customer who was selected on the quote header.
 * - The currency symbol is stored in the Customer table in the column "CustomerCurrency"
 *
 */

// FIXME START

// FIXME END