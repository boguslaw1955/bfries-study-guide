/**
 * User Story:
 * As a Sales Rep, I want to see the size of the Discount negotiated with the customer "indirectly" via entering the Requested Price.
 *
 * Acceptance Criteria / Implementation Details:
 * - this elements returns money value of Negotiated Discount in Customer's currency
 * - Negotiated Discount is calculated only, when the user entered the Required Price, otherwise we do not calculate it
 * - Negotiated Discount = SuggestedPrice - RequiredPrice
 *
 */

// the Negotiated Discount Amount is back-calculated, because the SalesRep negotiated a specific price to be used as Invoice Price
// Negotiated Discount Amount == (List - StandardDiscount%) - Invoice Price

// FIXME START

// FIXME END
