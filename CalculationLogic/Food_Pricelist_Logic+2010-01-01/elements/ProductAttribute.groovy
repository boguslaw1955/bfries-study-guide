/**
 * User Story:
 * - As a Pricing Manager, I can see which product attribute the products in the
 *  price list have.
 *
 * Acceptance Criteria / Implementation Details:
 *  - This element returns the product attribute for the given product.
 *  - If the product attribute is missing, a warning is added and null is returned.
 *  - The product attributes are given by the column "ProductAttribute" in the
 *      Product master data table.
 */

// FIXME START

// FIXME END