/**
 * Acceptance Criteria / Implementation Details:
 * - This element retrieves and returns the Country Markup %:
 * - The Country Markup % values are provided in the price parameter "CountryAdjustment".
 * - If the lookup fails, a warning is added and null is returned.
 * - The element output must be available as a Pricelist column.
 */

// FIXME START

// FIXME END