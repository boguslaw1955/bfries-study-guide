/**
 * This element will not have any code breaks in the exam.
 */

/**
 * User Story:
 * As a Sales Manager, I need to have on the quote line a name of the Sales Rep, who negotiated/edited the line items,
 * in order to have that name later in integration to another systems.
 *
 * Acceptance Criteria / Implementation Details:
 * - this element displays login of the Sales Rep who is now working with the Quote.
 */

return api.user().loginName