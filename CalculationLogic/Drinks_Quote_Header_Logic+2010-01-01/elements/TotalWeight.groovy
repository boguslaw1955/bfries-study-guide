/**
 * User Story:
 *  As a Sales Person, I want to see on the Quote Header the summarized information about the Total Weight (in kilograms) of the products from the Quote lines.
 *
 * Acceptance Criteria / Implementation Details:
 * - This element calculates and then shows on the Quote Header the information about the Total Weight (in kilograms) summarized from the line item information about product Weight and negotiated Quantity.
 * - The result output is named "TotalWeight" and labeled "Total Weight (kg)"
 * - Thh number should be displayed with suffix "kg"
 */

// Only add the warnings property if the array of warnings is not empty

// Needed in Freight Surcharge calculation

// FIXME START

// FIXME END